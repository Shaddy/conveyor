// use std::fmt;
// use std::sync::Arc;

// use super::{core, memory, symbols};

use super::iochannel::{Device};
// use super::symbols::parser::Error;


#[allow(dead_code, unused_variables)]
fn search_pattern(device: &Device, name: &str, bytes: Vec<u8>, neighbour: &str) -> Option<u64> {
    unimplemented!()
}